plugins {
    id "java"
}

group "org.example"
version "1.0-SNAPSHOT"

repositories {
    mavenCentral()
}

dependencies {
    // Change these paths to point to your Cantaloupe JAR.
    implementation(files("/Users/alexd/Projects/GitHub/cantaloupe-project/cantaloupe/target/cantaloupe-5.0-SNAPSHOT.jar"))
    testImplementation(files("/Users/alexd/Projects/GitHub/cantaloupe-project/cantaloupe/target/cantaloupe-5.0-SNAPSHOT.jar"))
    testImplementation(group: "org.junit.jupiter", name: "junit-jupiter", version: "5.4.2")
}
